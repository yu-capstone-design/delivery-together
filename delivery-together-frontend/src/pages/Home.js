import React from 'react';

const Home = () => {
  return (
    <div
      style={{
        height: '90%',
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <h1>Delivery Together에 오신것을 환영합니다.</h1>
    </div>
  );
};

export default Home;
